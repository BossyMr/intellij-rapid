package com.bossymr.rapid.language.psi;

import com.intellij.psi.PsiCodeFragment;

/**
 * A {@code RapidCodeFragment} represents a code fragment which outside the current project. For example, a text field
 * in a user interface.
 */
public interface RapidCodeFragment extends PsiCodeFragment {}
